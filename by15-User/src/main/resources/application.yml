spring:
  application:
    name: by15-User
server:
  port: 8701 # 服务提供方
  dataSource:
    driverClassName:
    url:
    username:
    password:
# 指定当前eureka客户端的注册地址,
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
  instance:
    hostname: localhost
